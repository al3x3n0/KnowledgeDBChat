# Minimal Python dependencies for the dedicated LaTeX compilation worker.
# Keep this intentionally small so the worker image does not pull heavy ML deps.

# Background Tasks
celery==5.3.4
redis==5.0.1

# Database (async)
sqlalchemy==2.0.23
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.12.1

# Storage
minio>=7.2.0

# Config / logging
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0
loguru==0.7.2
tenacity==8.2.3

