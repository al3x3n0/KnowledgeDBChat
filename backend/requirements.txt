# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
asyncpg==0.29.0
alembic==1.12.1

# Vector Database
qdrant-client>=1.9.0
sentence-transformers>=2.8.0
rank-bm25==0.2.2
huggingface_hub>=0.20.0

# LLM and AI
langchain==0.0.340
langchain-community==0.0.1
# langgraph>=0.2.0 is unsatisfiable alongside the pins above and makes the
# image impossible to build: langchain-community 0.0.1 requires
# langchain-core<0.1 while langgraph 0.2 requires langchain-core>=0.2.27. No
# resolver can satisfy both, so `pip install -r requirements.txt` fails
# outright. The local development environment where the test suite passes has
# no langgraph installed either, and langgraph_issue_pr_service already guards
# its import and reports the feature as unavailable.
#
# Re-enabling LangGraph means modernizing the langchain stack (langchain and
# langchain-community to 0.2+ lines), which is a dependency migration with its
# own testing — not something to bundle into an image change.
# langgraph>=0.2.0
ollama==0.1.7
anthropic>=0.69.0
openai>=1.60.0
transformers==4.35.2
torch==2.1.1
torchaudio==2.1.1

# Background Tasks
celery==5.3.4
redis==5.0.1

# Data Processing
beautifulsoup4==4.12.2
GitPython==3.1.40
python-multipart==0.0.6
aiofiles==23.2.1
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
openpyxl>=3.1.0
pypdf>=3.0.0
unstructured>=0.10.0
python-pptx>=0.6.21
python-docx>=0.8.11
mammoth>=1.6.0
markdown>=3.4.0
reportlab>=4.2,<5

# Video/Audio Transcription
openai-whisper>=20230918
yt-dlp>=2024.8.6
ffmpeg-python>=0.2.0
librosa>=0.10.0
speechbrain>=0.5.14
scikit-learn>=1.3.2
resemblyzer>=0.1.1

# Utilities
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0
httpx==0.25.2
tenacity==8.2.3
loguru==0.7.2
minio>=7.2.0
croniter>=2.0.3

# Security
python-jose[cryptography]==3.3.0
cryptography>=43,<45
passlib[bcrypt]==1.7.4
slowapi==0.1.9

# Directory auth (LDAP)
ldap3==2.9.1

# Web Scraping

# Development
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
aiosqlite==0.19.0
black==23.11.0
isort==5.12.0
flake8==6.1.0

# Email utilities (for parsing dates)
email-validator==2.1.0

# Custom tool dependencies
jsonpath-ng>=1.6.0
RestrictedPython>=7.0
Jinja2>=3.1.0
